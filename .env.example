# Copy to .env and fill in what you have. Everything is optional —
# the pipeline falls back to macOS voices + synthesized music without keys.

# Real TTS voices (biggest quality upgrade)
OPENAI_API_KEY=
# Premium TTS voices (used over OpenAI if set)
ELEVENLABS_API_KEY=

# Live script generation
ANTHROPIC_API_KEY=
XAI_API_KEY=
